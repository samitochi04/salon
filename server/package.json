{
  "name": "server",
  "version": "1.0.0",
  "description": "Express backend for Radiant Bloom salon bookings",
  "main": "src/server.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon src/server.js",
    "start": "cross-env NODE_ENV=production node src/server.js",
    "lint": "cross-env ESLINT_USE_FLAT_CONFIG=false eslint --config .eslintrc.cjs \"src/**/*.js\"",
    "lint:fix": "cross-env ESLINT_USE_FLAT_CONFIG=false eslint --config .eslintrc.cjs \"src/**/*.js\" --fix",
    "format": "prettier --check \"src/**/*.{js,json}\"",
    "format:fix": "prettier --write \"src/**/*.{js,json}\"",
    "test": "echo \"Add tests with npm run test\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.80.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1",
    "nodemailer": "^7.0.10",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "nodemon": "^3.1.10",
    "prettier": "^3.6.2"
  }
}
